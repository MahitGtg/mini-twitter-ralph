Completed tweet detail page task.

Changes:
- Added `src/app/tweet/[tweetId]/page.tsx` with loading and 404 states.
- Linked tweet content and timestamp to `/tweet/{id}`.
- Enhanced `getTweetById` to return author data; updated tests.

Tests not run.
