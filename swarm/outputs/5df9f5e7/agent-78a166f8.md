Completed profile display name editing.

- Added `name` handling in `updateProfile` mutation with trimming/clearing.
- Added display name field to profile editor and mutation call.
- Added backend tests for name update, trimming, and clearing.

Tests not run (not requested).
